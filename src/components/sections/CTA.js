import React from "react";

const CTA = () => {
  return (
    <section className="cta-section" id="contact">
      <h2 className="cta-title">Ready to Get Insured?</h2>
      <p className="cta-subtitle">
        Join thousands of happy customers who trust InsureFlow with their
        protection.
      </p>
      <button className="btn-cta">Start Your Journey Today →</button>
    </section>
  );
};

export default CTA;
